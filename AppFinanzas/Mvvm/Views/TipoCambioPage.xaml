<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewModels="clr-namespace:AppFinanzas.Mvvm.ViewModels"
             x:Class="AppFinanzas.Mvvm.Views.TipoCambioPage"
             Title="Tipo de Cambio">

    <!--<ContentPage.BindingContext>
        <viewModels:TipoCambioViewModel />
    </ContentPage.BindingContext>-->

    <CollectionView ItemsSource="{Binding TiposCambio}" Margin="15">
        <CollectionView.ItemTemplate>
            <DataTemplate>
                <Frame BackgroundColor="#F4F6F8"
                   CornerRadius="14"
                   Padding="15"
                   Margin="0,8"
                   HasShadow="True"
                   BorderColor="#DADDE1">
                    <VerticalStackLayout Spacing="5">
                        <Label Text="{Binding CasaFormateada}"
                           FontSize="20"
                           TextColor="#1F2E46"
                           FontAttributes="Bold" />

                        <HorizontalStackLayout>
                            <Label Text="💸 Compra:" FontAttributes="Bold" />
                            <Label Text="{Binding Compra, StringFormat=' {0:C}'}" />
                        </HorizontalStackLayout>

                        <HorizontalStackLayout>
                            <Label Text="💵 Venta:" FontAttributes="Bold" />
                            <Label Text="{Binding Venta, StringFormat=' {0:C}'}" />
                        </HorizontalStackLayout>

                        <Label Text="{Binding FechaActualizacion, StringFormat='📅 {0:dd/MM/yyyy HH:mm}'}"
                           FontSize="12"
                           TextColor="Gray"
                           HorizontalOptions="End" />
                    </VerticalStackLayout>
                </Frame>
            </DataTemplate>
        </CollectionView.ItemTemplate>
    </CollectionView>

</ContentPage>
